package co.edu.uceva.serviciopais;

import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

/**
 * Prueba de carga de contexto, que se realiza para asegurarse de que la aplicación se inicia correctamente y que todos
 * los componentes se cargan correctamente.
 */
@RunWith(SpringRunner.class)
@SpringBootTest
class ServicioPaisApplicationTests {

    /**
     * Prueba de carga de contexto, que se realiza para asegurarse de que la aplicación se inicia correctamente y que todos
     */
    @Test
    void contextLoads() { // Se comprueba que el contexto de la aplicación se carga correctamente.
    }

}
